# cantoreader-data

Public data endpoint for the CantoReader iOS app. Holds only `articles.json` —
the small (~74 KB) daily slice of graded Cantonese news rewrites the app fetches
over the air. Regenerated daily by CI in the private app repo. No app code or
private data lives here.
